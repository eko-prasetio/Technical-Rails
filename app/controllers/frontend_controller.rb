class FrontendController < ActionController::Base
    # layout :set_layout

    # private 

    # def set_layout 
    #     # if current_user && current_usr.admin == true
    #     # Ensures you won't error if not logged in when you check if admin
    #     if current_user&.admin? || current_user&.regular?
    #         'admin'
    #     else
    #         'frontend'
    #     end
    # end
end